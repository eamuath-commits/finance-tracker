version: '3.8'

services:
  db:
    image: postgres:15-alpine
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=finance_db
      - TZ=Asia/Riyadh
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - /etc/localtime:/etc/localtime:ro

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: always
    ports:
      - "8000:8000"
    depends_on:
      - db
    env_file:
      - path: ./backend/.env
        required: false
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/finance_db
      - TZ=Asia/Riyadh
      - GEMINI_MODEL=${GEMINI_MODEL:-gemini-2.5-flash}
      - ALRAJHI_API_KEY=${ALRAJHI_API_KEY:-}
      - ALRAJHI_API_SECRET=${ALRAJHI_API_SECRET:-}
      - ALRAJHI_REDIRECT_URI=${ALRAJHI_REDIRECT_URI:-https://qayd.io/api/bank/callback}
      - FRONTEND_URL=${FRONTEND_URL:-https://qayd.io}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./backend:/app

  sms-agent:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: always
    depends_on:
      - db
      - backend
    env_file:
      - path: ./backend/.env
        required: false
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/finance_db
      - TZ=Asia/Riyadh
      - GEMINI_MODEL=${GEMINI_MODEL:-gemini-2.5-flash}
    command: [ "python", "sms_agent.py" ]
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./backend:/app

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /etc/localtime:/etc/localtime:ro
      # Reloads on code change
    environment:
      - TZ=Asia/Riyadh

  cloudflared:
    image: cloudflare/cloudflared:latest
    restart: always
    command: tunnel --no-autoupdate run --token ${CLOUDFLARE_TUNNEL_TOKEN}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - backend
      - frontend

volumes:
  postgres_data:
